MariaDB [project_ARMS]> -- =============================================
MariaDB [project_ARMS]> -- Student Management Procedures
MariaDB [project_ARMS]> -- =============================================
MariaDB [project_ARMS]>
MariaDB [project_ARMS]> USE project_ARMS;
Database changed
MariaDB [project_ARMS]>
MariaDB [project_ARMS]> DELIMITER $$
MariaDB [project_ARMS]>
MariaDB [project_ARMS]> -- Procedure 1: Add New Student
MariaDB [project_ARMS]> CREATE PROCEDURE AddStudent(
    ->     IN p_reg_no VARCHAR(20),
    ->     IN p_NIC VARCHAR(20),
    ->     IN p_fName VARCHAR(50),
    ->     IN p_mName VARCHAR(50),
    ->     IN p_lName VARCHAR(50),
    ->     IN p_email VARCHAR(100),
    ->     IN p_dob DATE,
    ->     IN p_gender VARCHAR(5),
    ->     IN p_address VARCHAR(255),
    ->     IN p_batch VARCHAR(10),
    ->     IN p_status VARCHAR(30)
    -> )
    -> BEGIN
    ->     DECLARE v_user_id INT;
    ->
    ->     -- Insert into User table
    ->     INSERT INTO `User` (NIC, fName, mName, lName, email, dob, gender, address)
    ->     VALUES (p_NIC, p_fName, p_mName, p_lName, p_email, p_dob, p_gender, p_address);
    ->
    ->     -- Get the generated user_id
    ->     SET v_user_id = LAST_INSERT_ID();
    ->
    ->     -- Insert into Student table
    ->     INSERT INTO Student (reg_no, status, batch, user_id)
    ->     VALUES (p_reg_no, p_status, p_batch, v_user_id);
    ->
    ->     SELECT 'Student added successfully' AS message;
    -> END $$
Query OK, 0 rows affected (0.010 sec)

MariaDB [project_ARMS]>
MariaDB [project_ARMS]> -- Procedure 2: Update Student Status
MariaDB [project_ARMS]> CREATE PROCEDURE UpdateStudentStatus(
    ->     IN p_reg_no VARCHAR(20),
    ->     IN p_new_status VARCHAR(30)
    -> )
    -> BEGIN
    ->     UPDATE Student
    ->     SET status = p_new_status
    ->     WHERE reg_no = p_reg_no;
    ->
    ->     SELECT 'Student status updated' AS message;
    -> END $$
Query OK, 0 rows affected (0.003 sec)

MariaDB [project_ARMS]>
MariaDB [project_ARMS]> -- Procedure 3: Get Student Details
MariaDB [project_ARMS]> CREATE PROCEDURE GetStudentDetails(
    ->     IN p_reg_no VARCHAR(20)
    -> )
    -> BEGIN
    ->     SELECT
    ->         s.reg_no,
    ->         s.status,
    ->         s.batch,
    ->         u.fName,
    ->         u.mName,
    ->         u.lName,
    ->         u.email,
    ->         u.dob,
    ->         u.gender,
    ->         u.NIC,
    ->         u.address
    ->     FROM Student s
    ->     JOIN `User` u ON s.user_id = u.user_id
    ->     WHERE s.reg_no = p_reg_no;
    -> END $$
Query OK, 0 rows affected (0.003 sec)

MariaDB [project_ARMS]>
MariaDB [project_ARMS]> DELIMITER ;
MariaDB [project_ARMS]>